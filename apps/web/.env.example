# Runtime config is served from /api/config (see apps/web/app/api/config/route.ts).
# Set these on the *server* process, not as NEXT_PUBLIC_* build vars.
CONVKIT_SERVER_URL=http://localhost:4000
CONVKIT_WS_URL=ws://localhost:4000/ws
